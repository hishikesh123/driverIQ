# DriverIQ — Phase 6: Local LLM Interface

Generated by `python scripts/run_phase6.py --demo`. Model: `llama3.1:8b` via Ollama, temperature 0.

## The split

The model appears twice, on either side of the analytics engine, and never
in the middle:

```
question -> [parse] -> params -> recommend.py -> dict -> [narrate] -> answer
```

Python produces every number. The model only turns a question into parameters,
and a result into a sentence.

## Two guards

**Normalisation.** The parser returns things like `"evening"` where a number
was requested, so its output is repaired in Python before it reaches the engine:
time words map to clock hours, `"today"` resolves against the system clock, days
are validated, and hours are clamped. Anything unresolvable produces a clarifying
question rather than a guess.

**Faithfulness.** Every dollar figure in the generated answer is checked against
the source data before it is shown. On a mismatch the narration is discarded and
the deterministic figures are printed instead. A model narrating earnings will
eventually invent one; on a money-adjacent tool that has to be impossible to
surface, not merely unlikely. Only money is policed — counts and durations get
restated loosely in ordinary prose, and flagging those would bury the errors
that actually cost something.

## Exchanges

**Q — I have 4 hours free on Friday evening, is it worth going out?**

> You have 4 hours free on Friday evening. The likely total earnings for this time range from $24.93 to $77.54. The evening peak window, which is 3 hours long, is expected to earn between $20.61 and $72.98.

`source: llm` · `9.5s`

**Q — When is the best time to drive this week?**

> Thursday evening peak is likely to earn between $24.68 and $73.64, with a high confidence level based on 50 times worked before. Friday evening peak is also a good option, with a likely total range of $20.61 to $72.98, based on 54 times worked before. Sunday evening peak is another option, with a likely total range of $25.19 to $70.16, based on 42 times worked before.

`source: llm` · `7.0s`

**Q — How long should I stay out once I start?**

> You should stay out for 3 hours, as the number of trips per hour peaks in the second hour and holds through the third. After the third hour, the number of trips falls off.

`source: llm` · `3.5s`

**Q — Is Sunday afternoon any good?**

> Sunday afternoon is likely to be worth between $2.39 and $22.91. This is based on 39 times worked before with a high confidence level.

`source: llm` · `4.2s`

**Q — Which area should I drive in tonight?**

> I can't answer that one. This system only knows your own past earnings by day and time — it has no view of current demand, surge, other drivers, or which area to pick. Location specifically isn't answerable here: 27 of your 33 recorded sessions were in the same cell and 99% of your driving falls within 3 km of one point, so there's no second area to compare against. Ask me about timing instead.

`source: canned` · `0.5s`

**Q — Should I work Monday morning?**

> There is no history for Monday morning, so there is no evidence to suggest whether it's worth working. The driver has never worked this time before, and there's no data to estimate earnings.

`source: llm` · `4.2s`

## Run summary

- Questions: 6
- Answered by the model: 5
- Narrations rejected for inventing a figure: 0
- Mean response time: 4.8s (two model calls per question)

